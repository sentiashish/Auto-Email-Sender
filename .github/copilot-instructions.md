# Workspace Instructions

- This workspace contains a Next.js bulk email app.
- Keep the upload flow, recipient preview, and SMTP send route aligned.
- Prefer small, focused changes and validate with `npm run build` after edits.
- Use the existing design tokens and keep the UI consistent across screens.